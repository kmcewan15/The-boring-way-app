## Topic 1 

### step 2

Tutorial for installation:

https://claude.com/product/claude-code (reference this)

1. in terminal: https://code.claude.com/docs/en/quickstart#step-1-install-claude-code (include commands depending on OS) video placeholder
- check if exists via `claude --version` in terminal

2. as vsc extension - video placeholder
- check if exists -> covered in video

Get rid of "why this matters", keep the "careful" bit

what you'll do section - install claude code either via terminal or vsc extension, run claude --version to see what version is running

get rid of "you'll know it worked"

### step 3 

Instead of "authenticate" step it should be finding it. In vsc extension, a video demo placeholder where extension is accessed and authenticated via claude enterprise. In terminal, another video placeholder.

Keep the steps but summarise a bit more, since the video handles most of what to do, the steps are there for people who authenticate via different ways, claude enterprise, personal subscription etc, but keep it brief.

### step 4

Keep content, terminal instructiosn stay the same. For vsc extension section, anotehr placeholder video on how to open a directory. This section below is too long, compress or remove some content:

None of this is a requirement, by the way. An empty folder works. A folder with no version control in it works. More files will not make Claude faster either. The only thing that matters is that the files are yours, so you can tell at a glance whether an answer is right.

Why this matters
This is the test you will use for the rest of the course. You know your own project, so you are the one person who can catch Claude being confidently wrong about it. In a folder you have never seen, every answer looks plausible.

Careful
Pick a project you know well, but not the one carrying this quarter. You will be letting Claude change files in topic 3, and you want your first mistake to be a cheap one.

### Note on vsc extension vs terminal

Add appropriate collapsibles or some formatting for users to distinguish if they have installed the vsc extension or terminal and which path to follow in each substep.

## Topic 2-5

I think these topics are just far too verbose and boring. Add actual substance by progressing a mini project in each topic, makes it more interactive than just a reading or copy and paste tasks for each subtopic. 

Topic 1 and possibly 2 (im not sure) focuses on that base knowledge, where most subtopics have video demos for the user. Topics 3-5 can have video demos, but should be only few and placed carefully. Place placeholders appropriately for topic 2-5 and re-write content as I suggested to be less verbose/boring.

